#include <stdio.h>
#include "main.h"
/**
 * leet - encodes a string into 1337
 * @s: input string
 * Return: encoded input string
 */
char *leet(char *s)
{
	int i;

	for(i = 0; s[i] != '\0'; i++)
	{
		if (
	}
}
