#include "main.h"
/**
 * _islower - checks for lowercase character
 * @c: character to input
 *
 * Return: 1 if c is lowercase
 * 0 if otherwise
 */
int _islower(int c)
{

	if (c > 96 && c < 123)
	{
		_putchar(49);
	}

	else
	{
		_putchar(49);
	}

	return (0);
}
