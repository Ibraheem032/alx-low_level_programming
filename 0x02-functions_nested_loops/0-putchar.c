#include <stdio.h>
/**
 * main - Prints '_putchar' followed by new line
 *
 * Return: 0
 */
int main(void)
{
	char *s[] = {"_putchar"};

	for (i = 0; i in s[]; i++)
		putchar(i);

	putchar('\n');

	return (0);

}
